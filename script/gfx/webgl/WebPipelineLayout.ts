import { PipelineLayout, PipelineLayoutInfo } from "gfx-main";

export default class WebPipelineLayout implements PipelineLayout {
    initialize(info: PipelineLayoutInfo): boolean { return false; }
}