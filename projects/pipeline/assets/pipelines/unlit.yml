flows:
  - bindings:
      - ubo: Camera
        binding: 1
    stages:
      - name: forward
        phases:
          - pass: unlit
            visibility: ${DEFAULT}
          - pass: default
            visibility: ${UI}
