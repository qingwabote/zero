textures:
  - name: shadowmap
    usage:
      - DEPTH_STENCIL
      - SAMPLED
    extent: [1024, 1024]
ubos:
  - name: camera
    type: Camera
  - name: light
    type: Light
  - name: shadow
    type: Shadow
flows:
  - bindings:
      - ubo: camera
        binding: 1
      - ubo: light
        binding: 2
      - ubo: shadow
        binding: 3
      - texture: shadowmap
        binding: 0
    stages:
      - name: shadow
        phases:
          - pass: shadow
            visibility: ${DEFAULT}
        framebuffer:
          depthStencil: shadowmap
        clears:
          - DEPTH
      - name: forward
        phases:
          - pass: phong-shadow
            visibility: ${DEFAULT}
          - pass: default
            visibility: ${UI}
