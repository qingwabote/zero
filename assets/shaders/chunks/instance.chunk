struct Instance {
    mat4 model;
#if USE_SKIN
    vec4 joint;
#endif
};

#if USE_SKIN
    #define INSTANCE_NUM 204
#else
    #define INSTANCE_NUM 256
#endif

layout(set = 2, binding = 0) uniform PerDraw {
    Instance instances[INSTANCE_NUM];
};